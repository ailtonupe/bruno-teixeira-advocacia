backend:
  name: git-gateway
  branch: main

site_url: https://exquisite-panda-fc8801.netlify.app
display_url: https://exquisite-panda-fc8801.netlify.app

media_folder: public/imagens
public_folder: /imagens

locale: pt

collections:
  - name: artigos
    label: Artigos
    label_singular: Artigo
    folder: artigos
    create: true
    slug: "{{year}}-{{month}}-{{day}}-{{slug}}"
    editor:
      preview: false
    fields:
      - label: Título
        name: title
        widget: string

      - label: Data de Publicação
        name: date
        widget: datetime
        format: "YYYY-MM-DD"
        date_format: "DD/MM/YYYY"
        time_format: false

      - label: Resumo
        name: resumo
        widget: text

      - label: Categoria
        name: categoria
        widget: select
        options:
          - { label: "Direito Trabalhista", value: "trabalhista" }
          - { label: "Direito Cível", value: "civel" }
          - { label: "Direito Previdenciário", value: "previdenciario" }
          - { label: "Direito Criminal", value: "criminal" }
          - { label: "Dicas Jurídicas", value: "dicas" }
        default: "trabalhista"

      - label: Conteúdo
        name: body
        widget: markdown